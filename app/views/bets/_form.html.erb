<%= form_for(@bet) do |f| %>
  <% if @bet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bet.errors.count, "error") %> prohibited this bet from being saved:</h2>

      <ul>
      <% @bet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :user_id %>
  <%= f.hidden_field :week_id %>
  <div class="form-group">
    <%= label_tag "Números" %>
    <%= text_field_tag :numbers, nil, placeholder: "1, 2, 3, 4, 5",
      class: "form-control" %>
  </div>
  <div class="form-group">
    <%= label_tag "Estrelas" %>
    <%= text_field_tag :stars, nil, placeholder: "1, 2",
      class: "form-control" %>
  </div>
  <div class="action">
    <%= f.submit "Bet!", class: "btn btn-success" %>
  </div>
<% end %>
