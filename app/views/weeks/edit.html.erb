<h1>
  <% if params[:set] == 'solution' %>
    Adding solution
  <% elsif params[:set] == 'prize' %>
    Adding prize
  <% end %>
</h1>

<%= render 'form' %>

<%= link_to 'Show', @week %> |
<%= link_to 'Back', weeks_path %>
