<h1>Listing Transactions</h1>

<p>Here are the latest transactions associated with your account. This will include
bets, payments, prizes and any admin transactions that might be relevant</p>
<p>If your balance is in red and you feel like clearing it you can do so, by
making a transfer to the following account:</p>

<table class="table table-bordered table-condensed">
  <thead>
    <tr>
      <th>Kind</th>
      <th>Value</th>
      <th>User</th>
      <th>Details</th>
      <th>Date</th>
    </tr>
  </thead>

  <tbody>
    <% @transactions.each do |transaction| %>
      <tr>
        <td><%= transaction.kind_humanize %></td>
        <td><%= transaction.value %></td>
        <td><%= transaction.user.try(:name) %></td>
        <td><%= transaction.details %></td>
        <td><%= transaction.date %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% if current_user.is_admin? %>
  <%= link_to 'New Transaction', new_transaction_path %>
<% end %>
